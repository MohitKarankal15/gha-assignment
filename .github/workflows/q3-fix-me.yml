  
name: CI

on: 
  push
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs_on: ubuntu_latest_large
    steps:
      - name: List repository files
        run: ls-la
      - name: Run the hello script
        run: bash scripts/hello.sh

      - uses: actions/checkout
